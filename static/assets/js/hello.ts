// demo.ts
// Bu fayl faqat GitHub uchun qo‘shilgan, loyiha uni ishlatmaydi.

// Oddiy TypeScript misol
function greet(name: string): string {
    return `Hello, ${name}!`;
}

const user = "Django Developer";
console.log(greet(user));
